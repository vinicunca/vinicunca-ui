import { rAF } from '../../../../test-utils/tick';

describe('VDialog.vue', () => {
  test('render test', async () => {
    // const wrapper = mount(Dialog, {
    //   slots: defaultSlots,
    //   props: {
    //     modelValue: true,
    //   },
    // });

    // await nextTick();
    // await rAF();
    // await nextTick();
    // expect(wrapper.find('selector').text()).toEqual(AXIOM);
  });
});
